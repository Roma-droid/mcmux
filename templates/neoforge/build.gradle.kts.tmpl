// Generated by mcmux for target: {{version}} / {{loader}}
// Verify plugin & NeoForge versions against docs/VERSIONS.md and
// https://docs.neoforged.net/ before building.

plugins {
    id("net.neoforged.gradle.userdev") version "{{neoGradleVersion}}"
}

version = "{{modVersion}}"
group = "{{package}}"

java.toolchain.languageVersion.set(JavaLanguageVersion.of({{javaVersion}}))

runs {
    configureEach {
        systemProperty("forge.logging.markers", "REGISTRIES")
    }
    create("client")
    create("server")
}

dependencies {
    implementation("net.neoforged:neoforge:{{loaderVersion}}")
}
